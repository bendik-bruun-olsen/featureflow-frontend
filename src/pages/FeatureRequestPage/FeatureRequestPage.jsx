import { useNavigate } from "react-router-dom"

export default function FeatureRequestPage() {
    const navigate = useNavigate();

    const onClickRequest = () => {

    }

    return (
        <>
        <h1>Feature request page</h1>
        </>
    )
}